# DSA To C 化关键页面线框

本文档配合 [`docs/to-c-product-plan.md`](./to-c-product-plan.md) 输出 Phase 0 的 UI 线框，覆盖：

1. 登录 / 注册（`/login`、`/register`）— 已落地
2. 找回密码 / 重置密码（`/forgot-password`、`/reset-password`）— 已落地
3. 账户设置（`/account`）— 已落地（Phase 2）
4. 会员中心 / 升级（`/billing`）— 已落地（Phase 2，仅兑换码 / 兜底套餐目录，不接支付）
5. BYOK API Key 管理（`/account/api-keys`）— 已落地（Phase 4 后端 + 前端管理页，但 LLM 渠道路由层尚未根据 BYOK Key 切换）
6. 顶栏配额条（全局组件）— 已落地（位于 `SidebarNav` 内的 `QuotaIndicator`，复用桌面 / 移动端）
7. 超额引导对话框（全局组件）— 已落地（`QuotaExceededDialog` 监听 `dsa:quota-exceeded` 事件）
8. 首次引导（自选股 + demo 报告，登录后首跑）— 待建

> 线框使用 ASCII 表达布局，重点标注：组件来源、关键文案、调用的 API、关键交互。
> **不**包含像素级视觉规范；视觉细节沿用现有 `apps/dsa-web/src/components/common/` 中的 `Button` / `Input` / `Shell` / `SettingsAlert` 等基础组件，色彩沿用 `index.css` 中的 token。

## 1. 登录 / 注册（`/login`、`/register`）

> **状态**：已落地，对应 `apps/dsa-web/src/pages/UserAuthPage.tsx`。
> 本节作为后续迭代的对齐基线。

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│        ┌─────────────────────────────────────┐               │
│        │  [登录到 DSA]                        │               │
│        │  使用你的邮箱和密码登录, 享受 AI 自选股分析。 │       │
│        │                                     │               │
│        │  邮箱      [you@example.com      ]  │               │
│        │  密码      [••••••••••••       ]  │               │
│        │                                     │               │
│        │  [        登录 (主按钮)           ] │               │
│        │                                     │               │
│        │  忘记密码？        没有账号？立即注册 │               │
│        └─────────────────────────────────────┘               │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**注册态新增字段**：

```
确认密码  [••••••••••••       ]
邀请码    [仅当 USER_INVITE_CODES 非空时显示]
□ 我已阅读并同意《服务条款》《隐私政策》《投资风险免责声明》
```

**关键交互**：

- **API**：`POST /api/v1/account/login` / `POST /api/v1/account/register`，详见 [`docs/to-c-mode.md`](./to-c-mode.md) §3。
- **登录成功**：根据 `redirect` 查询参数返回；默认 `/`。
- **注册成功**：若 `USER_REQUIRE_EMAIL_VERIFICATION=true`，停留在当前页提示「请前往邮箱完成验证」；否则直接登录并跳转。
- **错误展示**：使用 `SettingsAlert variant="error"`，文案优先取后端 `ParsedApiError.message`。
- **提交锁**：`isSubmitting` 期间按钮文案改为「登录中…」/「注册中…」并禁用。

**待补充（Phase 1 收尾或 Phase 2）**：

- 注册时强制勾选「服务条款 / 隐私政策 / 投资风险免责声明」三选一聚合复选框；未勾选时禁用提交按钮。
- 第三方登录（微信扫码 / GitHub）只在确认运营策略后再加；MVP 不做。

## 2. 找回密码 / 重置密码

> **状态**：已落地，对应 `apps/dsa-web/src/pages/ForgotPasswordPage.tsx`。

**Step 1 — 发送重置邮件 (`/forgot-password`)**：

```
┌─────────────────────────────────────┐
│  [找回密码]                          │
│  输入注册邮箱, 我们会发送重置链接。     │
│                                     │
│  邮箱  [you@example.com    ]        │
│                                     │
│  [   发送重置邮件 (主按钮)        ]  │
│                                     │
│  返回登录                            │
└─────────────────────────────────────┘
```

- **API**：`POST /api/v1/account/request-password-reset`。
- **邮箱不存在**：后端不暴露存在性，前端统一显示「若该邮箱已注册, 重置邮件已发送, 请查收」。

**Step 2 — 用 token 重置（点击邮件后跳转 `/forgot-password?token=xxx`）**：

```
┌─────────────────────────────────────┐
│  [重置密码]                          │
│  请设置新密码。                       │
│                                     │
│  新密码      [••••••••••••     ]    │
│  确认密码    [••••••••••••     ]    │
│                                     │
│  [   重置并登录 (主按钮)         ]  │
└─────────────────────────────────────┘
```

- **API**：`POST /api/v1/account/reset-password`，成功后吊销该用户所有 session 并自动登录新 session。

## 3. 账户设置（`/account` — 待建）

替代当前 `SettingsPage` 中属于「个人」语义的字段。`SettingsPage` 仍然承担系统级配置（仅管理员可见）。

```
┌─────────────────────────────────────────────────────────────┐
│ Shell 顶栏: [DSA] [配额: 今日 3/5 次分析 · 1/5 次问股] [头像 ▼] │
├─────────────────────────────────────────────────────────────┤
│ 侧边页签                                                     │
│ ─────────────                                                │
│ ▶ 账户信息                                                   │
│   自选股 & 推送                                              │
│   API Key (BYOK)         [Pro 标签]                          │
│   登录设备                                                   │
│   危险操作                                                   │
├─────────────────────────────────────────────────────────────┤
│  [账户信息]                                                  │
│                                                             │
│  邮箱        you@example.com   [✓ 已验证]                   │
│  套餐        免费会员           [→ 升级到 Pro]               │
│  注册时间    2026-01-10                                      │
│  上次登录    2026-05-20 21:33 (来自 IP 223.xx.xx.xx)         │
│                                                             │
│  ─── 修改密码 ───                                            │
│  当前密码    [••••••••••••     ]                            │
│  新密码      [••••••••••••     ]                            │
│  确认新密码  [••••••••••••     ]                            │
│  [   保存并重新登录 (主按钮)   ]                              │
│                                                             │
│  ─── 修改邮箱 (Phase 4) ───                                  │
│  新邮箱      [new@example.com  ]                            │
│  [   发送验证邮件   ]                                         │
└─────────────────────────────────────────────────────────────┘
```

**关键交互**：

- **API**：
  - 读：`GET /api/v1/account/me`
  - 改密码：`POST /api/v1/account/change-password`（已上线，会吊销当前 session 并要求重新登录）
  - 改邮箱：Phase 4 新增 `POST /api/v1/account/change-email` + 二次验证邮件
- **危险操作**子页：注销账号、导出我的数据。注销需要二次邮箱验证 + 7 天冷静期；MVP 可以先只暴露「联系客服注销」入口。

## 4. 会员中心 / 升级（`/billing` — 待建）

```
┌─────────────────────────────────────────────────────────────┐
│ [会员中心]                                                   │
│ 当前套餐: 免费会员 · 不会过期                                  │
│ ▌升级到 Pro 解锁: 30 只自选股 · 50 次/天分析 · webhook 推送    │
├─────────────────────────────────────────────────────────────┤
│  [免费]            [Pro 月付]            [Pro 年付]          │
│  ¥0 / 永久          ¥XX / 月              ¥XXX / 年          │
│                                                             │
│  · 3 只自选股        · 30 只自选股          · 30 只自选股     │
│  · 5 次/天 分析      · 50 次/天 分析        · 50 次/天 分析   │
│  · 5 次/天 Agent     · 50 次/天 Agent       · 50 次/天 Agent  │
│  · 邮箱推送          · 邮箱 + Webhook       · 邮箱 + Webhook  │
│  · 平台基础模型      · 平台高级模型          · 平台高级模型    │
│  · -                · 可选 BYOK             · 可选 BYOK      │
│                                                             │
│  [当前档位]          [→ 升级 (主按钮)]      [→ 升级 (推荐)]  │
├─────────────────────────────────────────────────────────────┤
│ ─── 兑换码 ───                                               │
│ [        请输入兑换码        ]   [   兑换    ]                │
│                                                             │
│ ─── 订阅记录 ───                                              │
│ 2026-05-01  Pro 月付  来源: 兑换码 ABCD1234   到期: 2026-06-01│
│ 2026-04-01  Pro 月付  来源: 兑换码 WXYZ5678   到期: 2026-05-01│
└─────────────────────────────────────────────────────────────┘
```

**关键交互**：

- **API**（Phase 2 新增）：
  - `GET /api/v1/billing/plans`：列出当前可见套餐，价格 / 配额 / 能力开关。
  - `GET /api/v1/billing/subscription`：当前订阅 + 历史。
  - `POST /api/v1/billing/redeem`：用兑换码换 Pro。
  - `POST /api/v1/billing/checkout`（Phase 5）：创建支付订单。
- **MVP 路径**：免费 → 兑换码升 Pro；不显示「立即支付」按钮。
- **降级**：Pro 到期未续费自动回 free，所有过限的自选股保留只读但不参与调度。
- **文案规则**：所有「分析次数」措辞统一为「次/天」，避免次/月混用。

## 5. BYOK API Key 管理（`/account/api-keys` — 待建，Phase 4）

```
┌─────────────────────────────────────────────────────────────┐
│ [我的 API Key]              [Pro 专享]                       │
│ 使用自己的 Key 不占用平台配额, 但需自行承担调用成本。            │
├─────────────────────────────────────────────────────────────┤
│  Provider     状态     掩码                操作               │
│  ──────────   ─────    ────────────────    ─────────────     │
│  OpenAI       ✓ 启用    sk-****abcd         [测试] [禁用] [删除]│
│  Anthropic    ⚠ 失败    sk-ant-****wxyz     [测试] [启用] [删除]│
│  Gemini       未配置    -                   [+ 添加]          │
│  自定义       未配置    -                   [+ 添加]          │
│                                                             │
│  [+ 添加新 Key]                                              │
└─────────────────────────────────────────────────────────────┘
```

**新增 / 编辑表单**（弹窗或抽屉）：

```
Provider     [ OpenAI / Anthropic / Gemini / Anspire / 自定义(OpenAI 兼容) ]
名称(可选)   [我的备份 Key]
API Key      [sk-xxxxxxxxxxxxxx]
Base URL     [自定义 provider 才显示]
默认模型     [gpt-4o-mini ▼]   (按 provider 动态联动)
[ ✓ 设为该 provider 的默认 Key ]
[   测试连通性   ]   [   保存   ]
```

**关键交互**：

- **API**（Phase 4 新增）：
  - `GET /api/v1/account/api-keys`
  - `POST /api/v1/account/api-keys`：写入时直接走 `DATA_ENCRYPTION_KEY` 加密；返回值仅含掩码。
  - `POST /api/v1/account/api-keys/{id}/test`：复用现有 `LLM 渠道测试` 链路。
  - `DELETE /api/v1/account/api-keys/{id}`
- **安全规则**：表单提交后**绝不**回显原始 Key；编辑只能改名/默认模型/启用状态，重设 Key 必须重新输入。
- **错误处理**：测试失败时使用 `SettingsAlert variant="error"` + 错误码（参考 `docs/llm-providers.md` 的错误分类）。

## 6. 顶栏配额条（全局组件，待建）

挂在 `Shell` 顶栏右侧，所有登录后的页面可见。

```
[今日剩余: 分析 2/5 · Agent 4/5]
```

**变体**：

```
免费档剩余正常:    [今日剩余: 分析 2/5 · Agent 4/5]               (中性色)
免费档已用完:      [今日剩余: 分析 0/5 · Agent 0/5  → 升级]       (警告色, 点击 → /billing)
Pro 档剩余正常:    [今日剩余: 分析 35/50 · Agent 42/50]            (中性色)
Pro 档已用完:      [今日剩余: 分析 0/50 → 切换 BYOK]              (警告色, 点击 → /account/api-keys)
BYOK 模式启用:     [BYOK · 不计平台配额]                          (品牌色)
```

**关键交互**：

- **API**：`GET /api/v1/account/status` 已返回当日 `analysis` / `agent` 用量；后续可加入 `notify`。
- **刷新策略**：登录后初始化拉一次 + 每次完成分析 / Agent 调用后乐观刷新 + 顶栏定时（5 min）兜底刷新。
- **响应式**：移动端折叠为图标 + 数字徽章，点击展开 popover 显示完整明细。

## 7. 超额引导对话框（全局组件，待建）

当业务接口返回 `quota_exceeded` 错误码时（Phase 2 配额服务新增），全局拦截并弹出。

```
┌────────────────────────────────────────────┐
│  ⚠ 今日额度已用完                            │
│                                            │
│  你今天的「AI 分析」额度已用完 (5/5)。        │
│  明天 0 点重置, 也可以选择以下方式立即继续：   │
│                                            │
│  [→ 升级到 Pro (推荐)]                      │
│  [→ 配置我的 API Key (BYOK)]                │
│  [关闭]                                     │
└────────────────────────────────────────────┘
```

**关键交互**：

- **触发**：业务 API 返回 HTTP 402 + `error.code='quota_exceeded'` + `error.kind='analysis'|'agent'|'notify'`。
- **拦截层**：在 `apps/dsa-web/src/api/error.ts` 的 `parseApiError` 之后追加全局监听，由根组件接管展示，避免每个页面重复实现。
- **升级按钮**：跳转 `/billing` 并带 `?from=quota&kind=analysis`。
- **BYOK 按钮**：仅 Pro 可见，跳转 `/account/api-keys`。
- **降级**：若用户已是 BYOK 模式但仍触发，文案改为「你的 Key 调用失败, 请检查余额或权限」并跳 `/account/api-keys`。

## 8. 首次引导（登录后首跑，待建）

新注册用户首次进入 `/` 时，使用半屏 onboarding：

```
┌─────────────────────────────────────────────────────────────┐
│  Step 1 / 3                                                 │
│  欢迎使用 DSA · 你的个人股票 AI 分析助手                       │
│                                                             │
│  先选 1–3 只你最关心的股票, 我们立即生成一份决策报告。           │
│                                                             │
│  自选股  [+ 600519] [+ AAPL] [+ HK00700]                    │
│  搜索    [输入股票代码或名称…]                                 │
│                                                             │
│  [跳过]                              [下一步 →]              │
└─────────────────────────────────────────────────────────────┘
```

```
┌─────────────────────────────────────────────────────────────┐
│  Step 2 / 3                                                 │
│  开启每日推送 (可选)                                          │
│                                                             │
│  [✓] 每个交易日 18:00 给我发一封报告邮件                       │
│  [ ] 接受产品更新和优惠通知                                    │
│                                                             │
│  [上一步]                            [下一步 →]              │
└─────────────────────────────────────────────────────────────┘
```

```
┌─────────────────────────────────────────────────────────────┐
│  Step 3 / 3                                                 │
│  风险提示                                                    │
│                                                             │
│  本产品基于 AI 模型生成观点, 不构成投资建议。                   │
│  请基于自身情况独立决策, 投资风险自负。                         │
│                                                             │
│  [ ] 我已阅读并同意                                           │
│                                                             │
│                                       [开始体验 →]           │
└─────────────────────────────────────────────────────────────┘
```

**关键交互**：

- **首次判定**：`GET /api/v1/account/me` 返回 `onboarded=false` 时显示，完成后置为 `true` 并保存。
- **跳过策略**：用户跳过时仍然落库 `onboarded=true`，避免每次进首页都打扰。
- **数据写入**：自选股写入 per-user 自选股表（Phase 1 隔离落地后启用）。

## 9. 文案与合规要求

- **每页页脚**：固定显示「内容由 AI 生成, 仅供参考, 不构成投资建议。投资有风险, 入市需谨慎。」
- **每封邮件**：模板末尾追加同一句免责声明，并附「退订」链接（链向 `/account` 的「自选股 & 推送」页签）。
- **错误文案统一规范**：参考现有 `apps/dsa-web/src/api/error.ts`：
  - 网络错误：「网络异常, 请稍后重试」
  - 配额错误：「今日额度已用完」
  - 鉴权错误：「请先登录」
  - 服务端错误：「服务暂时不可用, 我们正在处理」

## 10. 待对齐事项

- 邀请码 / 兑换码是否共用同一张表？建议拆分：邀请码用于注册阶段（`USER_INVITE_CODES`），兑换码用于升级 Pro。
- 顶栏配额条是否在「未登录」状态也展示？建议**不**展示，未登录时顶栏改为登录 / 注册 CTA。
- BYOK 模式启用后，是否在 Header 直接展示「BYOK」徽标？建议展示，便于用户识别当前调用走自己的 Key。
- 移动端 / 桌面端是否复用相同 onboarding？MVP 复用，后续根据数据再分支。
